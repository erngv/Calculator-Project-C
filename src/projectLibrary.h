#ifndef PROJECTLIBRARY_H_INCLUDED
#define PROJECTLIBRARY_H_INCLUDED
#include <stdio.h>
#endif

// Function Prototypes
float validateInput(float userInput);                                                       // for validation

float perimeterSquare(float side);                                                          // for perimeter of square

float areaSquare(float side);	                                                            // for area of square

float volumeCube(float side);	                                                            // for volume of cube

float perimeterRectangle(float length, float width);	                                    // for perimeter of rectangle

float areaRectangle(float length, float width);	                                            // for area of rectangle

float volumeRectangularPrism(float length, float width, float height);	                    // for volume of rectangular prism

float perimeterTriangle(float sideA, float sideB, float sideC);	                            // for perimeter of triangle

float areaTriangle(float base, float height);	                                            // for area of triangle

float volumeTriangularPyramid(float pyramidHeight, float lengthBase, float widthBase);      // for volume of triangular pyramid

float perimeterCircle(float radius);	                                                    // for perimeter of circle

float areaCircle(float radius);	                                                            // for area of circle

float volumeSphere(float radius);	                                                        // for volume of sphere
